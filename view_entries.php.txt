<!DOCTYPE html>
<html>
<head>
    <title>📚 Guest Book Entries</title>
    <style>
        body { font-family: Arial; padding: 30px; }
        .entry {
            background: #f9f9f9;
            padding: 15px;
            border-left: 5px solid #2196F3;
            margin-bottom: 20px;
            white-space: pre-wrap;
        }
    </style>
</head>
<body>
    <h2>📚 Guest Book Entries</h2>
    <?php
    $file = "entries.txt";

    if (file_exists($file)) {
        $entries = explode("---\n", file_get_contents($file));

        foreach ($entries as $entry) {
            $trimmed = trim($entry);
            if (!empty($trimmed)) {
                echo "<div class='entry'>" . htmlspecialchars($trimmed) . "</div>";
            }
        }
    } else {
        echo "<p>No entries yet.</p>";
    }
    ?>
    <br>
    <a href="index.php">📝 Sign Again</a>
</body>
</html>
